# OpenStreetMap input

Place the Austrian 2025 OpenStreetMap extract at `austria-2025.osm.pbf`.

This file is used only by `SDG/Generation.ipynb` to sample synthetic company locations from building footprints. Routing and motorway-exit construction use the annual GIP snapshots in `OGD/GIP/`. The PBF file is intentionally excluded from Git.
